## 🐍 Техническая Документация Скрипта `Загрузка распоряжений с портала правовой информации`

-----

### 📝 Обзор

Данный скрипт на Python предназначен для **автоматического сбора метаданных и скачивания PDF-документов** с официального российского портала правовой информации **publication.pravo.gov.ru** через его API. Скрипт использует пагинацию для обхода всех страниц результатов поиска и сохраняет найденные документы в указанную локальную папку.

### ✨ Основные Возможности

  * **Извлечение Метаданных:** Собирает информацию о документах (номер, название, дата, орган) со всех страниц поиска.
  * **Пагинация:** Автоматически обрабатывает многостраничные результаты поиска, определяя общее количество страниц.
  * **Скачивание PDF:** Формирует URL для скачивания PDF по уникальному идентификатору (`eoNumber`) и сохраняет файл.
  * **Обработка Файлов:** Создает локальную папку для сохранения и формирует безопасные, информативные имена файлов.
  * **Прогресс и Отчетность:** Использует `tqdm` для визуализации процесса скачивания и предоставляет итоговую статистику.
  * **Защита от Повторного Скачивания:** Пропускает файлы, которые уже существуют в папке назначения.
  * **Обработка Ошибок:** Включает обработку сетевых ошибок (таймауты, ошибки HTTP-статуса) и ошибок парсинга JSON.

-----

### ⚙️ Зависимости

Для работы скрипта требуются следующие библиотеки:

  * **`requests`**: Для выполнения HTTP-запросов к API и скачивания файлов.
  * **`json`**: Для работы с данными в формате JSON.
  * **`os`**: Для работы с файловой системой (создание папки, формирование путей).
  * **`tqdm`**: Для отображения прогресс-бара во время скачивания.
  * **`time`**: Для добавления задержек между запросами (для снижения нагрузки на сервер).

Установка зависимостей:

```bash
pip install requests tqdm
```

-----

### 🧱 Структура Кода

Скрипт состоит из трех основных частей:

1.  **`download_documents()`**: Основная функция для **сбора метаданных И скачивания** PDF-файлов.
2.  **`get_documents_info_only()`**: Вспомогательная функция для **только сбора метаданных** (без скачивания).
3.  **`if __name__ == "__main__":`**: Точка входа в программу, которая вызывает `download_documents()` по умолчанию.

-----

### 1️⃣ Функция `download_documents()`

#### 📌 Назначение

Осуществляет полный цикл: запрос к API, получение метаданных со всех страниц и скачивание каждого документа в формате PDF.

#### 🛠️ Параметры Запроса (Пример)

Параметры определяют критерии поиска документов. Они устанавливаются в словаре `params` перед первым запросом.

| Параметр | Значение (Пример) | Описание |
| :--- | :--- | :--- |
| `DocumentTypes` | `7ff5b3b5-3757-...` | **UUID типа документа** (например, Указ Президента). |
| `SignatoryAuthorityId`| `8005d8c9-4b6d-...` | **UUID органа** (например, Президент РФ). |
| `DocumentDateFrom` | `16.10.2025` | Начальная дата диапазона документа. |
| `DocumentDateTo` | `31.10.2025` | Конечная дата диапазона документа. |
| `PageSize` | `30` | **Количество документов на одной странице.** |
| `index` | `1` | Текущий номер страницы. |
| `SortedBy` | `6` | Тип сортировки. |

#### 🔄 Логика Работы

1.  **Инициализация:** Устанавливаются базовый URL API `http://publication.pravo.gov.ru/api/Documents`, параметры поиска (`params`) и папка для скачивания (`download_dir`).
2.  **Первый Запрос:** Выполняется первый запрос для получения общих данных о поиске: `total_count` (всего документов), `pageSize` (размер страницы) и `pagesTotalCount` (общее количество страниц).
3.  **Сбор Метаданных:** Цикл `for` проходит от 1 до `pagesTotalCount`. На каждой итерации:
      * Обновляется параметр `"index"` (номер страницы).
      * Выполняется запрос к API.
      * Полученные документы (`'items'`) добавляются в общий список `all_items`.
      * Вводится пауза в **0.5 секунды** (`time.sleep(0.5)`).
4.  **Скачивание:** Запускается второй цикл `for` по всем собранным документам (`all_items`).
      * Формируется URL для скачивания: `http://publication.pravo.gov.ru/file/pdf?eoNumber={eo_number}`.
      * Генерируется **безопасное имя файла** на основе номера, названия и даты.
      * Проверяется существование файла: если файл уже есть, он пропускается.
      * Выполняется HTTP-запрос с потоковой передачей (`stream=True`).
      * Файл сохраняется локально с использованием `tqdm` для отображения прогресса.
      * Вводится пауза в **1 секунду** (`time.sleep(1)`) между скачиваниями.
5.  **Отчет:** Выводится итоговая статистика: общее количество, успешно скачано, не удалось скачать.

-----


### 🚀 Использование

Скрипт по умолчанию выполняет функцию `download_documents()`.

**Для скачивания документов (по умолчанию):**

```python
if __name__ == "__main__":
    download_documents()
    # get_documents_info_only() # Закомментировано
```

**Для получения только информации:**

```python
if __name__ == "__main__":
    # download_documents() # Закомментировать
    get_documents_info_only() # Раскомментировать
```
